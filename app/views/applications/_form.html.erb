<%= form_for(@application, :html => {:multipart => true})  do |f| %>
  <% if @application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
      <% @application.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

        <div class="field">
          <%= f.label :name %><br />
          <%= f.text_field :name %>
        </div>


        <div class="field">
          <%= f.label :version %><br />
          <%= f.text_field :version %>
        </div>

        <div class="field">
          <%= f.label :installer_url %><br />
          <%= f.text_field :installer_url %>
        </div>


        <div class="field">
          <%= f.label "Application Icon" %><br />
          <%= f.file_field :image%>
        </div>




  <div class="actions">
    <%= f.submit  'Next' %>
  </div>
<% end %>
